import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'firebaseTest4MergeImg',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
